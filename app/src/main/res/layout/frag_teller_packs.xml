<LinearLayout android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:orientation="vertical"
    android:background="@drawable/shape_rect13"
    tools:context="com.skylightapp.Tellers.TPackListFragment"
    xmlns:android="http://schemas.android.com/apk/res/android">

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:paddingStart="@dimen/activity_horizontal_margin"
        android:paddingTop="10dp"
        android:text="My Package Overview"
        android:id="@+id/package_tittle"
        android:textStyle="italic"
        android:layout_marginBottom="12dp"
        android:textColor="@color/colorPrimaryDark"
        android:textSize="16sp" />


    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recycler_view_packs"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        tools:listitem="@layout/packs_item_row"
        android:clipToPadding="false"
        android:scrollbars="vertical" />

    <com.github.clans.fab.FloatingActionButton
        android:layout_width="wrap_content"
        android:id="@+id/cm_home_pack"
        android:background="@drawable/shape_rect_7"
        android:layout_gravity="end"
        app:fab_shadowRadius="10dp"
        android:src="@drawable/ic_home_black_24dp"
        android:screenReaderFocusable="true"
        android:layout_height="wrap_content"/>

</LinearLayout>