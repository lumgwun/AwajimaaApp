<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/shape_rect14"
    tools:context="com.skylightapp.Admins.AdminLoanList">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical">

        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:paddingStart="6dp"
            android:paddingTop="10dp"
            android:text="ALL loans"
            android:textAlignment="center"
            android:textStyle="bold"
            android:id="@+id/loanText3"
            android:layout_marginBottom="12dp"
            android:textColor="@color/colorPrimaryDark"
            android:textSize="20sp" />

        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/recycler_loan_admin2"
            android:layout_width="match_parent"
            tools:itemCount="@android:integer/status_bar_notification_info_maxnum"
            android:layout_height="wrap_content"
            tools:listitem="@layout/loan_teller_row"
            android:clipToPadding="false"
            android:scrollbars="vertical" />
    </LinearLayout>
</ScrollView>